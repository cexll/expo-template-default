#!/usr/bin/env sh
set -e

# Avoid relying on broken `node_modules/.bin/*` shims in this environment.
# Execute the real entrypoints directly.
cd "$(dirname "$0")/.."

node ./node_modules/lint-staged/bin/lint-staged.js
